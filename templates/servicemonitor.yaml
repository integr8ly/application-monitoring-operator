apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    application-monitoring: 'true'
  name: grafana
  namespace: {{ .Namespace }}
spec:
  endpoints:
    - port: grafana
      scheme: http
  selector:
    matchLabels:
      application-monitoring: 'true'